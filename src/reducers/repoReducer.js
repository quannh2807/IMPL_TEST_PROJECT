const initalState = { repoItems: [] };

const repoReducer = (state = initalState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default repoReducer;
